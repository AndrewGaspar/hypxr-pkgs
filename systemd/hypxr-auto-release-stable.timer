[Unit]
Description=Auto-release stable mirror every 6 hours

[Timer]
# Stable promotion is deliberately manual and hardware-gated. This timer is
# shipped only as an explicit operator opt-in; bin/setup does not enable it.
OnCalendar=*-*-* 01:00:00 America/New_York
Persistent=true
