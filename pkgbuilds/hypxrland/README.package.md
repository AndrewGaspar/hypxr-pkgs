# HypXRland Arch package notes

The pinned mpv stereo metadata helper is installed at
`/usr/share/hypxrland/mpv/mpv-hypxr-stereo.lua`. Load it with
`mpv --script=/usr/share/hypxrland/mpv/mpv-hypxr-stereo.lua`, or link it into
`~/.config/mpv/scripts/` to load it automatically.
